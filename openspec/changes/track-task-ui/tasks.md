## 1. Factory 后端代理

- [ ] 1.1 在 `app.py` 的 `proxy_tracks` 路由中新增 `/api/proxy/tracks/<int:track_id>/keywords` 路径匹配
- [ ] 1.2 验证代理路由返回正确关键词数据

## 2. 前端采集弹窗改造

- [ ] 2.1 在弹窗顶部新增"采集模式"切换（赛道 / 自定义），使用 radio 或 toggle 按钮
- [ ] 2.2 赛道模式下加载赛道列表（`GET /api/proxy/tracks`）并填充下拉框
- [ ] 2.3 选择赛道后自动填充主题（只读）和数据源（只读展示）
- [ ] 2.4 选择赛道后加载并展示关键词 chips（`GET /api/proxy/tracks/{id}/keywords`）
- [ ] 2.5 关键词区域限高 120px + overflow-y scroll
- [ ] 2.6 切换回自定义模式时恢复主题和数据源为可编辑

## 3. 提交逻辑改造

- [ ] 3.1 赛道模式下 `saveCollect()` 提交 `{track_id, cron_expr, max_results}`（不含 subject/sources）
- [ ] 3.2 自定义模式下保持原有提交逻辑不变
- [ ] 3.3 编辑赛道任务时自动切换到赛道模式并预填数据

## 4. 任务列表展示

- [ ] 4.1 任务列表中赛道任务显示赛道颜色标签
- [ ] 4.2 需要从 `/api/proxy/tracks` 获取赛道颜色映射缓存到前端

## 5. 验证

- [ ] 5.1 创建赛道任务（新材料），确认自动填充 + 关键词展示
- [ ] 5.2 创建自定义任务（核电），确认行为与改动前一致
- [ ] 5.3 编辑赛道任务，确认正确切换到赛道模式
- [ ] 5.4 任务列表正确显示赛道标签和自定义任务

## 6. 提交推送

- [ ] 6.1 提交并推送所有变更
