## 1. 任务列表增加类型列

- [x] 1.1 在 `loadTasks()` 的采集任务表头增加"类型"列（位于"主题"之后）
- [x] 1.2 根据 track_id 是否存在，渲染蓝色"赛道采集"或灰色"自定义采集" badge
- [x] 1.3 移除任务行中赛道 badge 旁边的"管理关键词"按钮，只保留纯展示的赛道 badge

## 2. 赛道页面增加关键词管理入口

- [x] 2.1 在 `loadTrack()` 的赛道卡片中增加"🔑 管理关键词"按钮
- [x] 2.2 按钮点击时调用 `showKeywordManager(trackId, trackName)` 弹出关键词管理模态框
- [x] 2.3 确保 `showKeywordManager` 函数在赛道页面上下文中可调用（全局函数，已具备）

## 3. 集成验证

- [x] 3.1 部署到服务器，重启 factory 服务
- [x] 3.2 验证任务列表：类型列显示正确，管理按钮已移除
- [x] 3.3 验证赛道页面：每个卡片有管理入口，点击弹出模态框，可增删关键词
- [x] 3.4 提交并推送代码变更
