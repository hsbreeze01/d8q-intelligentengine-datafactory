# Tasks: Remove Deprecated get_db() Definition

## 1. Code Cleanup

- [ ] Remove the deprecated `get_db()` function and its `# DEPRECATED` comment from `app.py` (lines 72–76)

## 2. Verification

- [ ] Run `ruff check app.py` — no new warnings
- [ ] Run `pytest` — all tests pass
